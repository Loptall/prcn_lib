/*
グラフのコンストラクタの実装について重み付きかそうでないか、有向か無向か、が要素としてある。
そもそもの表現としては隣接リスト表記を用いるが、その内部構造としては上の要素について公平であるので
コンストラクトの時点でユーザーの指定を受け実装するのが最善。
そこで上記要素についてマーカーとなるenuｍを作成し、それを引数としてコンストラクタを実装するが、
後置の(usize, usize)について重みなしでは重みの要素が省略されていることが想定できるので、
一括で実装しようとすると可変引数が必要となる。でもそんなものRustにはない！
マクロが必要！どうしようどうしようもうちょっとUserInterface変えるべきかな
そろそろマクロについて学ぶべきかもしれない
*/

pub struct Graph ;