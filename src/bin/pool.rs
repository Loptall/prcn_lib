fn main() {
    // let mut uf = UnionFind::<usize>::new(5);

    // assert!(!uf.equiv(0, 1));
    // assert!(!uf.equiv(2, 1));
    // assert!(!uf.equiv(2, 3));
    // assert!(!uf.equiv(0, 4));

    // uf.union(0, 1);

    // assert!(uf.equiv(0, 1));
    // assert!(uf.equiv(1, 0));

    // uf.union(0, 3);
    // uf.union(2, 4);

    // assert!(uf.equiv(0, 3));
    // assert!(uf.equiv(4, 2));
    // assert!(!uf.equiv(4, 3));

    // uf.union(4, 3);

    // assert!(uf.equiv(3, 4));
    // assert!(uf.equiv(3, 0));

    println!("{}", 3usize.next_power_of_two());
}
